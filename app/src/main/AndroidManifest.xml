<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.memoworld.majama">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Majama">


        <activity android:name=".User.UserNameInput">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".User.Account_Details"></activity>
        <activity android:name=".LoginInstructionSplash.NewLogin"></activity>


        <activity android:name=".User.Interest.Interest">

        </activity>

        <activity android:name=".LoginInstructionSplash.SignUpActivity" />
        <activity android:name=".LoginInstructionSplash.login" />
        <activity android:name=".LoginInstructionSplash.SplashActivity"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>